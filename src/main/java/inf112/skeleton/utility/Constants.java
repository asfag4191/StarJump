package inf112.skeleton.utility;

public final class Constants {
    /**
     * Pixels Per Meter (PPM) is the conversion factor used to translate between
     * graphical units (pixels) and physics units (meters) in the Box2D physics world.
     * 32 pixels = 1 meter.
     */
    public static final float PPM = 32;
}
